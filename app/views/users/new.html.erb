<%= form_for @user do |form| %>
	<div>
	<%= form.label "Your name" %>
	<%= form.text_field :name, required: true, minlength: 6 %>
	</div>
	<div>
	<%= form.label "I dynamically find shared local network drives. Is yours on this list? No? Try to share it and reload the page." %>
	<%= form.select :videos_path, @video_paths, { include_blank: true } %>
	</div>
	<%= form.submit "Submit" %>
<% end %>